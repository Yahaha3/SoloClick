# Windows 键盘连点器需求文档（Revision 2）

## 1. 项目概述

- **项目名称**：SoloKeyClicker (原 SoloClick)
- **目标平台**：Windows
- **项目目标**：实现一个以**键盘按键连点**为核心，鼠标连点为辅的高性能连点工具。
- **主要特性**：多任务并行/互斥、独立按键配置、配置导入导出。

---

## 2. 核心功能需求

### 2.1 连点控制（核心）

- **多任务支持**：支持同时设置多个按键的连点任务（例如：按A连点A，按B连点B）。
- **连点对象**：
    - **键盘按键**（主要）：任意键盘按键（A-Z, 0-9, F1-F12等）。
    - **鼠标按键**（次要）：左键、右键、中键。
- **频率设置**：每个按键任务可**独立设置**连点频率（毫秒间隔）。
- **触发模式**：每个任务可独立设置：
    1.  **开关模式 (Toggle)**：按一下开始连点，再按一下停止。
    2.  **按压模式 (Hold)**：按住时持续连点，松开即停止。
- **独立/互斥模式**：
    - **全局开关**：是否开启“独立模式”。
    - **逻辑**：开启独立模式后，若启动某个任务（无论是Toggle还是Hold），会自动停止其他正在运行的任务。关闭某个任务则不影响其他（即不会自动恢复之前的）。

### 2.2 配置管理

- **配置列表**：界面展示当前所有配置好的连点任务列表。
- **导入/导出**：
    - 支持将当前的任务列表配置导出为文件（如 JSON）。
    - 支持从文件导入配置，覆盖或追加当前列表。

### 2.3 热键与快捷操作

- **自定义热键**：实际上就是“连点对象”本身作为触发热键（例如：配置“按A连点A”时，A就是热键）。
- **紧急停止**：全局热键（如 F12）停止所有正在运行的任务。

---

## 3. GUI 界面需求

### 3.1 界面结构

- **任务列表区**：显示当前添加的所有连点任务（按键、频率、模式、状态）。
- **任务编辑区**（或弹窗）：
    - 选择按键（捕获按键输入）。
    - 设置频率（毫秒）。
    - 选择触发模式（开关/按压）。
    - 按钮：添加/更新、删除。
- **全局控制区**：
    - “独立模式”复选框。
    - 导入/导出配置按钮。
    - 紧急停止说明。

### 3.2 交互体验

- **按键捕获**：在设置按键时，按下键盘即可自动填入键名，无需手动输入文本。
- **状态反馈**：列表中的任务应显示当前是否正在运行（Running/Idle）。

---

## 4. 非功能需求

### 4.1 性能与稳定性

- **高并发处理**：支持多个按键同时连点，互不卡顿。
- **低延迟**：按压模式下，松开按键应立即停止，无明显延迟。
- **防抖动**：处理好 Windows 键盘事件的重复触发问题（Auto-repeat），避免 Hold 模式下逻辑混乱。

### 4.2 兼容性

- **管理员权限**：必须请求管理员权限以确保在游戏等高权限窗口中生效。

---

## 5. 异常与边界场景

- **按键冲突**：当“连点按键”与“触发按键”相同时（例如按A连点A），需要防止无限递归触发（Self-triggering protection）。通常底层模拟点击不会再次触发 hook，但需验证。
- **输入框输入**：在编辑框输入文字时，不应触发连点功能（需要检测焦点或设置启用/禁用开关）。

---

## 6. 交付物

- **源码**：Python 脚本。
- **可执行文件**：打包后的 exe（可选）。
- **配置文件示例**：默认的 JSON 配置。
